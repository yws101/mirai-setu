package com.blrabbit.mirai

import com.blrabbit.mirai.Util.MySetting
import net.mamoe.mirai.console.command.CommandSender
import net.mamoe.mirai.console.command.SimpleCommand
import net.mamoe.mirai.contact.User

/*object SetSetuKey : SimpleCommand(
    MiraiSetuMain ,"setkey", "设置lolicon的APIKEY",
    description = "设置lolicon的APIKEY",
) {
    @Handler // 标记这是指令处理器  // 函数名随意
    suspend fun CommandSender.handle(ApiKey: String) { // 这两个参数会被作为指令参数要求
        MySetting.APIKEY = ApiKey
        APIKEY = ApiKey
        sendMessage("设置APIKEY成功")
    }
}

object SetMasterID: SimpleCommand(
    MiraiSetuMain ,"setmaster", "设置主人id",
    description = "设置色图插件主人id",
) {
    @Handler // 标记这是指令处理器  // 函数名随意
    suspend fun CommandSender.handle(ID:Long) { // 这两个参数会被作为指令参数要求
        MySetting.masterid = ID
        sendMessage("设置主人id成功")
    }
}*/
